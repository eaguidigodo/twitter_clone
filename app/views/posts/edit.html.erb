<div class="tweetBox">
<form action="/posts" accept-charset="UTF-8" data-remote="true" method="put">
    <div class="tweetbox__input">
    <%=image_tag('avatar.jpg') %>
        <input type="text" placeholder="What's happening?" name="post[content]" value="<%= @post.content%>"> 
    </div>
    <button class="tweetBox__tweetButton" data-disable-with="">Tweet</button>
</form>
</div>
<% if @post.errors.any? %>
<div id="error_explanation">
    <h2><%=@post.errors.count %> There are errors! </h2>
    <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><=% msg %></li>
        <% end %>
    </ul>
</div>
<% end %>